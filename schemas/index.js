const contactAddSheme = require('./contacts');
const contactUpdateFavoriteShema = require('./contacts');
const contactUpdateSchema = require('./contacts');

const registerSchema = require('./users');
 const   loginSchema = require('./users');



module.exports = {
    contactAddSheme, 
    contactUpdateFavoriteShema,
    contactUpdateSchema,
    registerSchema,
    loginSchema,
  }